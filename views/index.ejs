<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-4Q6Gf2aSP4eDXB8Miphtr37CMZZQ5oXLH2yaXMJ2w8e2ZtHTl7GptT4jmndRuHDT" crossorigin="anonymous">
    <title>My Awesome Blog</title> <link rel="stylesheet" href="/styles/style.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Libre+Baskerville:ital,wght@0,400;0,700;1,400&family=Open+Sans:ital,wght@0,300..800;1,300..800&display=swap" rel="stylesheet">
</head>

<body>
    <section id="navbar">
        <nav class="navbar navbar-expand-lg navbar-dark bg-black" aria-label="Fifth navbar example">
            <div class="container-fluid">
                <a class="navbar-brand" href="/">Blog</a> <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarsExample05"
                    aria-controls="navbarsExample05" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarsExample05">
                    <ul class="navbar-nav mb-2 mb-lg-0 margin-left-overwrite">
                        <li class="nav-item"> <a class="nav-link active" aria-current="page" href="/">Home</a> </li>
                        <li class="nav-item"> <a class="nav-link" href="/create">Create New Post</a> </li>
                        <li class="nav-item"> <a class="nav-link" href="/about">About</a> </li>
                        <li class="nav-item"> <a class="nav-link disabled" aria-disabled="true">Categories</a> </li>
                    </ul>
                </div>
            </div>
        </nav>
    </section>

    <section id = "typical-bg">
        <!-- hehe just for cute background -->
    </section>

    <div class="container my-5">
        <h1 class="text-center mb-4">Welcome to My Blog!</h1>
        <p class="lead text-center text-muted">A collection of thoughts, ideas, and stories.</p>
        <div class="text-center mb-5">
            <a href="/create" class="btn btn-primary btn-lg">Start Writing Your Own Blog Post!</a>
        </div>

        <hr class="my-5">

        <h2 class="mb-4">Latest Blog Posts</h2>
        <section>
            <% if (items.length > 0) { %>
                <div class="row">
                    <% for (let i = 0; i < items.length; i++) { %>
                        <div class="col-12 col-md-6 col-lg-4 mb-4">
                            <div class="card h-100 shadow-sm">
                                <% if (items[i].blogImage) { %>
                                    <img src="<%= items[i].blogImage %>" class="card-img-top" alt="Blog Post Image">
                                <% } %>
                                <div class="card-body d-flex flex-column">
                                    <h3 class="card-title text-primary"><%= items[i].blogTitle%></h3>
                                    <p class="card-text text-secondary mb-3 flex-grow-1"><%= items[i].blogContent %></p>
                                    <div class="mt-auto">
                                        <a href="/edit/<%= items[i].id%>" class="btn btn-outline-primary btn-sm me-2">Edit</a>
                                        <form action="/delete/<%= items[i].id%>" method="POST" style="display: inline;">
                                            <button type="submit" class="btn btn-outline-danger btn-sm">Delete</button>
                                        </form>
                                    </div>
                                </div>
                            </div>
                        </div>
                    <% } %>
                </div>
            <% } else { %>
                <p class="lead text-muted text-center">No blog posts to display yet. Click "Start Writing" above to create your first post!</p>
            <% } %>
        </section>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-j1CDi7MgGQ12Z7Qab0qlWQ/Qqz24Gc6BM0thvEMVjHnfYGF0rmFCozFSxQBxwHKO"
        crossorigin="anonymous"></script>
</body>

</html>